public interface Engine {
    
  void eco(); 
    
    //In interfaces all methods are "public" by default. no need to type public
    // No need to type "abstract", bcs it is "abstract" by default in Interface
    // you must override "eco()" method in child..
}